# WebDAV -- HTTP face on the same share as Samba/FileBrowser (dav.lab). nginx's WebDAV module
# serves GET/PUT/DELETE/MKCOL/COPY/MOVE (no POST) over volumes/share via config/nginx/nginx.conf.
# Runs as 100:101 (Samba's smbuser) so files interoperate across SMB/HTTP. No auth.
services:
  webdav:
    image: nginx:1.27-alpine
    container_name: webdav
    restart: unless-stopped
    user: "100:101"
    volumes:
      - ../volumes/share:/srv
      - ../config/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - caddy
    labels:
      caddy: dav.${LAB_DOMAIN}
      caddy.reverse_proxy: "{{upstreams 8080}}"
      homepage.group: Storage & Packages
      homepage.name: WebDAV (curl)
      homepage.icon: /icons/nginx.png
      homepage.href: https://dav.${LAB_DOMAIN}
      homepage.description: curl GET/PUT/DELETE to the share
      homepage.weight: "25"
