# offline dev / crypto / network utilities (https://tools.lab).
services:
  it-tools:
    image: corentinth/it-tools@sha256:8b8128748339583ca951af03dfe02a9a4d7363f61a216226fc28030731a5a61f  # :latest pinned 2026-06
    container_name: it-tools
    restart: unless-stopped
    networks:
      - caddy
    labels:
      caddy: tools.${LAB_DOMAIN}
      caddy.reverse_proxy: "{{upstreams 80}}"
      homepage.group: Tools
      homepage.name: IT-Tools
      homepage.icon: /icons/it-tools.png
      homepage.href: https://tools.${LAB_DOMAIN}
      homepage.description: Dev / crypto / network utilities, offline
      homepage.weight: "20"
